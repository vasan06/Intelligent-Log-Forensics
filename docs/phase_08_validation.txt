PHASE 8 - SELECTIVE BACKEND DYNAMISM VALIDATION

Status: PASSED on 2026-08-09.

Scope rule
- Database evidence, scores, counts, dates, IPs, incidents, mappings, and file metadata are data.
- Titles, navigation, headings, button text, empty-state copy, and help text are labels.
- Only data elements are fetched from /api/v1; labels remain plain template text.

Validation and cross-checking
- Dashboard/history/evidence/incidents/report pages contain static loading targets and labels; their
  database values are fetched from the endpoints recorded in dynamism_audit.md.
- Analyst name/role moved to /api/v1/auth/me; attack education stays static knowledge content.
- Rendered upload/history HTML did not contain the uploaded filename while /api/v1/uploads did,
  demonstrating the value crosses the visible API boundary.
- Template search found no upload/risk/incident/log database field rendering.
- Broad suite remained at 16 passing tests after conversion.
