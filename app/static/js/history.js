const esc = value => String(value ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);
fetch("/api/v1/uploads").then(response => response.json()).then(rows => {
  document.getElementById("uploadHistory").innerHTML = rows.length ? rows.map(item => `<tr><td><strong>${esc(item.file_name)}</strong><small>${esc(item.file_type.toUpperCase())} · ${(item.file_size/1024).toFixed(1)} KB</small></td><td><span class="status ${esc(item.processing_status)}">${esc(item.processing_status)}</span></td><td>${item.quality ? item.quality.quality_score.toFixed(1) : "—"}</td><td>${item.valid_records} / ${item.total_records}</td><td>${new Date(item.upload_time).toLocaleString()}</td><td><a class="icon-button" href="/logs/${item.id}">Open</a></td></tr>`).join("") : '<tr><td colspan="6" class="table-empty">No uploads yet.</td></tr>';
}).catch(() => { document.getElementById("uploadHistory").innerHTML = '<tr><td colspan="6" class="table-empty">Evidence could not be loaded.</td></tr>'; });
