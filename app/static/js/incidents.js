const incidentPage = document.getElementById("incidentPage");
const esc = value => String(value ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);
fetch(`/api/v1/incidents/${incidentPage.dataset.fileId}`).then(r => r.json()).then(data => {
  document.getElementById("incidentFile").textContent = data.file_name;
  document.getElementById("incidentList").innerHTML = data.incidents.length ? data.incidents.map(item => `<article class="incident"><div class="incident-summary"><div><span class="severity ${esc(item.severity.toLowerCase())}">${esc(item.severity)}</span><h2>${esc(item.title)}</h2><p>${esc(item.summary)}</p></div><strong class="risk-number">${Math.round(item.score)}<small>risk</small></strong></div><div class="timeline">${item.events.map(event => `<div class="timeline-event"><span class="timeline-dot"></span><time>${esc(event.event_time ? new Date(event.event_time).toLocaleTimeString() : "Unknown time")}</time><div><strong>${esc(event.event_type)}</strong><p>${esc(event.description)}</p>${event.mitre_tactic ? `<span class="mitre-tag">${esc(event.mitre_tactic)} · ${esc(event.mitre_technique)}</span>` : ""}</div></div>`).join("")}</div></article>`).join("") : '<div class="empty-state"><h3>No incidents correlated</h3><p>The analyzed evidence did not cross the current risk thresholds.</p></div>';
}).catch(console.error);
